import { Pizza, PizzaCommon } from './Pizza';

const calabresa: PizzaCommon = {
  flavor: 'Calabresa',
  slices: 8,
};

const marguerita: Pizza = {
  flavor: 'Marguerita',
  slices: 6,
};

const nutela: Pizza = {
  flavor: 'Nutela',
  slices: 4,
};

console.log(calabresa);
console.log(marguerita);
console.log(nutela);
